# v3.0.0 发布准备 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补齐 CHANGELOG、CI（push/PR 测试 + tag 自动 Release）、README Web 优先重构（含真实截图），为 v3.0.0 发布做好准备。

**Architecture:** 新增 `scripts/extract_changelog.py`（TDD）从 Keep a Changelog 格式的 `CHANGELOG.md` 提取版本段；`.github/workflows/ci.yml` 跑 pytest，`release.yml` 在 `v*` tag 推送时复用测试并 `gh release create`；README「快速开始」Web 前置详写配 browser-use 自动截图两张。

**Tech Stack:** Python 3.13 / pytest / GitHub Actions / Flask（仅截图时启动）/ browser-use

**Spec:** `docs/superpowers/specs/2026-09-05-v3-release-design.md`（含 CHANGELOG 完整草稿，本计划附录不重复时以 spec 附录 A 为准，但下文已内联完整内容）

**工作分支:** `release/v3.0.0`（已基于 main@8eebf49 创建并切换）

---

### Task 1: CHANGELOG 段落提取脚本（TDD）

**Files:**
- Create: `scripts/extract_changelog.py`
- Test: `tests/test_extract_changelog.py`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_extract_changelog.py`：

```python
"""CHANGELOG 段落提取器单元测试。

被测对象: scripts/extract_changelog.py
"""
import sys
from pathlib import Path

import pytest

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "scripts"))

from extract_changelog import extract_section, main, normalize_tag  # noqa: E402

SAMPLE = """# 更新日志

## [未发布]

（新变更先积累在此段落，发布时改写为版本号并补充日期。）

## [3.0.0] - 2026-09-05

### 新增

- FIT 导出器

### 修复

- Keep 步数校验

## [2.1.1] - 2026-06-08

### 新增

- 东区操场轨迹

## [2.0.0] - 2026-05-15

### 新增

- Web 应用
"""


def test_extract_target_section():
    section = extract_section(SAMPLE, "3.0.0")
    assert "FIT 导出器" in section
    assert "Keep 步数校验" in section
    # 不包含相邻版本与未发布段的内容
    assert "东区操场轨迹" not in section
    assert "新变更先积累" not in section
    assert "Web 应用" not in section


def test_unreleased_does_not_leak():
    section = extract_section(SAMPLE, "2.1.1")
    assert "东区操场轨迹" in section
    assert "新变更先积累" not in section


def test_missing_version_raises():
    with pytest.raises(ValueError, match="9.9.9"):
        extract_section(SAMPLE, "9.9.9")


def test_no_false_prefix_match():
    # 版本 3.0.0 不得误匹配 3.0.01
    text = "## [3.0.01] - 2026-10-01\n\n内容\n"
    with pytest.raises(ValueError, match="3.0.0"):
        extract_section(text, "3.0.0")


def test_empty_section_raises():
    text = "## [1.0.0] - 2020-01-01\n\n## [2.0.0] - 2020-02-02\n\n内容\n"
    with pytest.raises(ValueError, match="内容为空"):
        extract_section(text, "1.0.0")


def test_normalize_tag():
    assert normalize_tag("v3.0.0") == "3.0.0"
    assert normalize_tag("3.0.0") == "3.0.0"
    assert normalize_tag("  v2.1.1 ") == "2.1.1"


def test_main_accepts_v_prefix(tmp_path, monkeypatch, capsys):
    changelog = tmp_path / "CHANGELOG.md"
    changelog.write_text(SAMPLE, encoding="utf-8")
    import extract_changelog as ec

    monkeypatch.setattr(ec, "CHANGELOG", changelog)
    assert ec.main(["v3.0.0"]) == 0
    assert "FIT 导出器" in capsys.readouterr().out


def test_main_exit_code_on_missing(tmp_path, monkeypatch, capsys):
    changelog = tmp_path / "CHANGELOG.md"
    changelog.write_text(SAMPLE, encoding="utf-8")
    import extract_changelog as ec

    monkeypatch.setattr(ec, "CHANGELOG", changelog)
    assert ec.main(["v9.9.9"]) == 1
    assert "9.9.9" in capsys.readouterr().err
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_extract_changelog.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'extract_changelog'`（收集阶段即报错）

- [ ] **Step 3: 写最小实现**

创建 `scripts/extract_changelog.py`（`scripts/` 目录不存在则一并创建；不加 `__init__.py`，测试通过 `sys.path` 注入导入）：

```python
#!/usr/bin/env python3
"""从 CHANGELOG.md 提取指定版本段落，用作 GitHub Release 正文。

用法:
    python scripts/extract_changelog.py <tag> [--output FILE]

tag 形如 v3.0.0 或 3.0.0（自动去掉 v 前缀）。段落为
"## [版本号]" 标题行到下一个 "## [" 标题行之间的内容；
顶部 "## [未发布]" 段因不带版本号而永远不会被命中。
找不到版本段或段落为空时，向 stderr 输出错误并以退出码 1 结束。
"""
from __future__ import annotations

import argparse
import re
import sys
from pathlib import Path

CHANGELOG = Path(__file__).resolve().parent.parent / "CHANGELOG.md"


def normalize_tag(tag: str) -> str:
    """'v3.0.0' -> '3.0.0'（不带前缀的保持原样）。"""
    return tag.removeprefix("v").strip()


def extract_section(text: str, version: str) -> str:
    """返回 text 中 [version] 段落内容（不含本段标题与下一段标题）。

    Raises:
        ValueError: 版本段不存在，或段落内容为空。
    """
    pattern = re.compile(rf"^## \[{re.escape(version)}\](\s|$)")
    lines = text.splitlines()
    start = None
    for i, line in enumerate(lines):
        if pattern.match(line):
            start = i + 1
            break
    if start is None:
        raise ValueError(f"CHANGELOG 中未找到版本段 [{version}]")
    body: list[str] = []
    for line in lines[start:]:
        if line.startswith("## ["):
            break
        body.append(line)
    section = "\n".join(body).strip()
    if not section:
        raise ValueError(f"版本段 [{version}] 内容为空")
    return section


def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(
        description="从 CHANGELOG.md 提取指定版本段落",
    )
    parser.add_argument("tag", help="git tag 名，如 v3.0.0")
    parser.add_argument(
        "--output",
        type=Path,
        default=None,
        help="写入指定文件而非 stdout",
    )
    args = parser.parse_args(argv)

    try:
        section = extract_section(
            CHANGELOG.read_text(encoding="utf-8"), normalize_tag(args.tag)
        )
    except (OSError, ValueError) as exc:
        print(f"错误: {exc}", file=sys.stderr)
        return 1

    if args.output is not None:
        args.output.write_text(section + "\n", encoding="utf-8")
    else:
        print(section)
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_extract_changelog.py -q`
Expected: `8 passed`

- [ ] **Step 5: 跑全量测试确认无回归**

Run: `python -m pytest -q`
Expected: `50 passed`（原 42 + 新 8）

- [ ] **Step 6: Commit**

```bash
git add scripts/extract_changelog.py tests/test_extract_changelog.py
git commit -m "feat: 新增 CHANGELOG 版本段提取脚本（Release 正文来源）"
```

---

### Task 2: CHANGELOG.md 全量落地

**Files:**
- Create: `CHANGELOG.md`

- [ ] **Step 1: 写入完整 CHANGELOG**

创建 `CHANGELOG.md`，内容如下（与 spec 附录 A 一致）：

```markdown
# 更新日志

本项目所有显著变更记录于此。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

（新变更先积累在此段落，发布时改写为版本号并补充日期。）

## [3.0.0] - 2026-09-05

### 新增

- FIT 导出器：基于 garmin-fit-sdk 输出含完整步频/步数数据流的 FIT 文件 (#8, #10)
- 步频/步数自动生成：按配速与里程生成符合真实分布的步频曲线，TCX 导出同步写入 (#8)
- Web 轨迹编辑器：高德地图标点与画笔绘制、橡皮擦、撤销/重做、z20 深度缩放，保存即入轨迹库 (#3)
- Web 前端重构为专业工作台风格 (#7)
- 指定日期批量生成（CLI 与 Web UI）(#9)
- 内置 campusrunning-cli Agent Skill，AI 编码工具对话式直接调用 (#6)
- 新增东区南操场轨迹
- 项目采用 MIT License

### 变更

- **默认输出格式从 TCX 改为 FIT**：Keep 导入推荐 FIT（含步频/步数数据）；需要 TCX 时加 `--format tcx` (#8, #10)

### 修复

- Keep 导入时步数/步幅校验不通过的问题 (#8)
- 指定日期批量生成的阻断问题（code review 发现）(#9)

## [2.1.1] - 2026-06-08

### 新增

- 东区操场轨迹

### 变更

- 最早/最晚开始时间的粒度从小时精确到分钟

### 修复

- issue #1：部分情况下不能正确生成文件的问题

## [2.1.0] - 2026-05-28

### 新增

- TimeRangePlanner 时间范围规划器
- 时间间歇跑、时间跑两类预设模板
- 西一区操场轨迹

## [2.0.0] - 2026-05-15

### 新增

- Flask Web 应用：表单化操作，与 CLI 生成结果一致
- 配速波动机制：热身 → 稳定 → 疲劳 → 冲刺四阶段，拒绝匀速直线

### 变更

- CLI 入口重构为子命令形式（daily / total / single）

## [1.0.0] - 2025-12-05

### 新增

- 首个版本：三种生成模式（每日范围 / 总公里数 / 单文件）
- 基于操场坐标的顺时针 GPS 轨迹生成与 GCJ-02 坐标修正
- 轨迹库与预设模板系统
- TCX 导出
```

- [ ] **Step 2: 用提取脚本验证真实文件**

Run: `python scripts/extract_changelog.py v3.0.0`
Expected: stdout 输出 `### 新增` 到 `- 指定日期批量生成的阻断问题（code review 发现）(#9)` 的段落，不含 `[未发布]` 与 `[2.1.1]` 内容

Run: `python scripts/extract_changelog.py v2.0.0`
Expected: 输出 Flask Web 应用与配速波动两条

Run: `python scripts/extract_changelog.py v9.9.9; echo "exit=$?"`
Expected: stderr 有 `未找到版本段 [9.9.9]`，`exit=1`

- [ ] **Step 3: Commit**

```bash
git add CHANGELOG.md
git commit -m "docs: 补齐 CHANGELOG（v1.0.0 → v3.0.0 全量）"
```

---

### Task 3: requirements.txt 与 CI workflows

**Files:**
- Create: `requirements.txt`
- Create: `.github/workflows/ci.yml`
- Create: `.github/workflows/release.yml`

- [ ] **Step 1: 创建 requirements.txt**

```
flask
garmin-fit-sdk
pytest
```

- [ ] **Step 2: 创建 .github/workflows/ci.yml**

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
      - run: pip install -r requirements.txt
      - run: python -m pytest
```

- [ ] **Step 3: 创建 .github/workflows/release.yml**

```yaml
name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
      - run: pip install -r requirements.txt
      - run: python -m pytest
      - run: python scripts/extract_changelog.py "${GITHUB_REF_NAME}" --output release_notes.md
      - env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release create "${GITHUB_REF_NAME}" --title "${GITHUB_REF_NAME}" --notes-file release_notes.md
```

- [ ] **Step 4: 本地验证 YAML 语法**

Run: `pip install pyyaml -q && python -c "import yaml; [yaml.safe_load(open(p, encoding='utf-8')) for p in ['.github/workflows/ci.yml', '.github/workflows/release.yml']]; print('YAML OK')"`
Expected: `YAML OK`（pyyaml 仅本地验证用，不加入 requirements.txt）

- [ ] **Step 5: Commit**

```bash
git add requirements.txt .github/workflows/
git commit -m "ci: 新增测试工作流与 tag 自动 Release（正文取自 CHANGELOG）"
```

---

### Task 4: Web 界面截图（browser-use 自动化）

**Files:**
- Create: `assets/readme/web_workbench.png`
- Create: `assets/readme/track_editor.png`

前置说明：轨迹编辑器默认 Leaflet「经典」档**无需高德 Key**；页面关键元素 id——画笔 `#tool-draw`、适配视野 `#fit-btn`、地图档切换 `#mode-classic/#mode-fresh/#mode-satellite`。

- [ ] **Step 1: 后台启动 Flask 应用**

Run（后台）: `python app.py`
等待就绪: `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5000` → `200`

- [ ] **Step 2: 截取工作台首页**

调用 browser-use skill：
1. 打开 `http://127.0.0.1:5000/`，视口设 1600×1000
2. 等待表单与轨迹下拉加载完成（「生成模式」「轨迹」等表单元素可见）
3. 截图保存为 `assets/readme/web_workbench.png`

- [ ] **Step 3: 截取轨迹编辑器**

调用 browser-use skill：
1. 打开 `http://127.0.0.1:5000/track-editor`
2. 等待 Leaflet 瓦片加载（地图区域无灰块）
3. 点击 `#tool-draw` 切换画笔模式
4. 沿地图上可见的操场跑道（贵阳某高校操场，位于初始视野附近；若无跑道可见，点 `#fit-btn` 适配视野后定位）依次点击 8~12 个点，围出环线——顶点连线与右上距离读数出现
5. 截图保存为 `assets/readme/track_editor.png`

- [ ] **Step 4: 人工核对两张截图**

用 Read 工具查看两张 PNG，确认：表单/工具栏完整可见、轨迹环线闭合、无加载失败的灰块、无遮挡弹窗。不合格则重复 Step 2/3。

- [ ] **Step 5: 停止应用并 Commit**

停止后台 `python app.py` 进程。

```bash
git add assets/readme/web_workbench.png assets/readme/track_editor.png
git commit -m "docs: 新增 Web 工作台与轨迹编辑器截图"
```

---

### Task 5: README 重构（Web 优先）

**Files:**
- Modify: `README.md:30-35`（五层机制）
- Modify: `README.md:39-66`（快速开始整节）
- Modify: `README.md:85`（添加轨迹方式一配图）
- Modify: `README.md:129-136`（文档列表）

- [ ] **Step 1: 「它能做什么」四层 → 五层**

Edit `README.md`，old_string：

```
每条记录由四层机制撑起「真实感」：

- **真实轨迹**——基于操场坐标生成顺时针 GPS 轨迹点，内置多条校园轨迹，也可自行添加
- **真实配速**——热身 → 稳定 → 疲劳 → 冲刺四阶段配速波动，拒绝匀速直线
- **坐标修正**——GCJ-02 系统性偏移自动校正，轨迹落在它该在的位置
```

new_string：

```
每条记录由五层机制撑起「真实感」：

- **真实轨迹**——基于操场坐标生成顺时针 GPS 轨迹点，内置多条校园轨迹，也可自行添加
- **真实配速**——热身 → 稳定 → 疲劳 → 冲刺四阶段配速波动，拒绝匀速直线
- **真实步频步数**——按配速与里程生成符合真实分布的步频曲线，FIT 与 TCX 均写入
- **坐标修正**——GCJ-02 系统性偏移自动校正，轨迹落在它该在的位置
```

- [ ] **Step 2: 替换「快速开始」整节（Web 前置）**

Edit `README.md`：将「## 快速开始」标题起、至「### 导入手机」标题之前的全部内容（现 README 第 39~76 行，含旧 `### CLI` 与 `### Web` 两小节及 Skill 引用块）整体替换为：

````
## 快速开始

要求 Python 3.13+，安装依赖：

```bash
pip install flask garmin-fit-sdk
```

### Web（推荐）

```bash
python app.py    # 浏览器访问 http://127.0.0.1:5000
```

![Web 工作台](assets/readme/web_workbench.png)

表单化操作，所见即所得：

- **三种生成模式**——每日范围 / 总公里数 / 单文件，另支持指定日期批量补记录
- **模板**——表单参数一键存成模板，下次直接套用
- **轨迹编辑器**——高德地图上标点、画笔绘制环线，实时查看距离，保存即入轨迹库
- **结果打包**——生成的 FIT/TCX 一键 ZIP 下载

### CLI

```bash
# 最快验证：生成一次 5 km 记录
python main.py single --date 2025-01-01 --distance 5.0

# 整月按每日范围生成
python main.py daily --start-date 2025-01-01 --end-date 2025-01-31 --min-km 2 --max-km 5

# 组合：模板 + 指定轨迹 + 打包 ZIP
python main.py daily --template easy_run --track campus_default --zip \
  --start-date 2025-01-01 --end-date 2025-01-07 --min-km 2 --max-km 5
```

完整参数说明见 [API 参考](docs/api_reference.md)。

### 让 AI 替你跑

仓库内置 Agent Skill（`.claude/skills/campusrunning-cli/`，软链入口在 `.agents/skills/`）。用 Claude Code 等 AI 编码工具打开本项目，说一句「帮我生成 9 月整月、每天 2~5 km 的跑步数据」即可——选命令、配参数这些事交给 AI，无需费脑。
````

「### 导入手机」小节原样保留。

- [ ] **Step 3: 「添加轨迹」方式一配图**

Edit `README.md`，old_string：

```
**方式一：Web 轨迹编辑器（推荐）**——启动 Web 后从首页进入「轨迹编辑器」，在高德地图上标点绘制轨迹、实时查看环线距离，保存后直接写入 `config/tracks/`，无需手工拾取坐标。
```

new_string：

```
**方式一：Web 轨迹编辑器（推荐）**——启动 Web 后从首页进入「轨迹编辑器」，在高德地图上标点绘制轨迹、实时查看环线距离，保存后直接写入 `config/tracks/`，无需手工拾取坐标。

![轨迹编辑器](assets/readme/track_editor.png)
```

- [ ] **Step 4: 文档列表追加 CHANGELOG**

Edit `README.md`，old_string：

```
- [Keep 导入教程](guied.md)
```

new_string：

```
- [Keep 导入教程](guied.md)
- [更新日志](CHANGELOG.md)
```

- [ ] **Step 5: 核对渲染**

通读修改后的 README，确认：图片路径与 Task 4 产出文件名一致；无残留旧 CLI 长代码块；「### 导入手机」未被误删；markdown 嵌套代码块（```` 包裹 ````）渲染正确。

- [ ] **Step 6: Commit**

```bash
git add README.md
git commit -m "docs: README 快速开始 Web 优先重构并补充截图与步频能力"
```

---

### Task 6: 全量验证与 PR

**Files:** 无新文件

- [ ] **Step 1: 全量测试**

Run: `python -m pytest -q`
Expected: `50 passed`

- [ ] **Step 2: 防御性检查（业务文件未被误改）**

Run: `python -m py_compile app.py main.py && git status --short`
Expected: 编译无输出；`git status` 无 `src/`、`web/`、`main.py`、`app.py` 的改动

- [ ] **Step 3: 提取脚本终验**

Run: `python scripts/extract_changelog.py v3.0.0 | head -3`
Expected: 输出以 `### 新增` 开头的 v3.0.0 段落

- [ ] **Step 4: 推送并创建 PR**

```bash
git push -u origin release/v3.0.0
gh pr create --title "chore: v3.0.0 发布准备（CHANGELOG / CI 自动 Release / README Web 优先）" --body "$(cat <<'EOF'
## 变更内容

- **CHANGELOG**：全量补齐 v1.0.0 → v3.0.0，Keep a Changelog 格式，顶部保留 [未发布] 段
- **CI**：push/PR 自动跑 pytest；推送 v* tag 自动创建 GitHub Release（正文取自 CHANGELOG 对应版本段，自研 scripts/extract_changelog.py，8 个测试用例）
- **README**：快速开始 Web 优先重构（真实截图两张：工作台首页、轨迹编辑器），CLI 精简为三条示例，「让 AI 替你跑」独立小节，真实感机制补第五层「真实步频步数」
- **requirements.txt**：新增依赖清单（CI 用）

## 测试

- [x] `python -m pytest` 50 passed（42 旧 + 8 新）
- [x] 提取脚本对 v3.0.0 / v2.0.0 / 不存在版本的行为验证
- [x] 两张截图人工核对
- [ ] CI 在 GitHub 上首次运行通过（合并前确认）
- [ ] 合并后打 `v3.0.0` tag 验证自动 Release（合并后确认）

## 发布序列（合并后）

```bash
git checkout main && git pull
git tag v3.0.0 && git push origin v3.0.0   # 自动触发 Release
```
EOF
)"
```

- [ ] **Step 5: 观察 CI 首跑**

Run: `gh pr checks --watch`
Expected: `CI / test` 通过（本 PR 的 push 同时触发 ci.yml）

注意：本 PR 不会触发 release.yml（只推分支未推 tag）。打 tag 发版在 PR 合并后由维护者执行（PR 描述已写明命令），不在本计划内自动执行。

---

## Self-Review 记录

- **Spec 覆盖**：CHANGELOG（Task 2）、提取脚本+测试（Task 1）、requirements/ci/release（Task 3）、截图（Task 4）、README 五处修订（Task 5，对应 spec 3 节全部修订点）、发布序列（Task 6 + PR 描述）——spec 各节均有对应任务
- **占位符扫描**：所有代码/文本步骤内联完整内容；截图步骤给出确切 URL、元素 id、动作序列
- **类型一致性**：`extract_section(text, version) -> str`、`normalize_tag(tag) -> str`、`main(argv) -> int` 在 Task 1 测试与实现、Task 2/6 调用中签名一致；图片文件名 `web_workbench.png`/`track_editor.png` 在 Task 4 产出与 Task 5 引用一致
